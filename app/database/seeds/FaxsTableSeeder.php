<?php

class FaxsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('faxs')->truncate();

		$faxs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('faxs')->insert($faxs);
	}

}
